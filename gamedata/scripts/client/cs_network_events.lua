-- cs_network_events.lua
-- Custom network events go here

-- Example:
-- packet_funcs.get_position = function(packet)
--   send_request(player_id, "get_position", player_position, PACKET_TYPE.NUMBER, function(packet)
--     -- handle response packet here
--   end)
-- end
